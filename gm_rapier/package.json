{
  "name": "gm_rapier",
  "version": "1.0.0",
  "description": "",
  "main": "dist/server.js",
  "scripts": {
    "start": "node dist/server.js",
    "build": "tsc",
    "dev": "ts-node src/server.ts",
    "clean": "rm -rf dist",
    "deploy:preview": "npm run clean && npm run build && gcloud app deploy --no-promote",
    "deploy": "npm run clean && npm run build && gcloud app deploy --verbosity=debug",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@dimforge/rapier3d": "^0.14.0",
    "@dimforge/rapier3d-compat": "^0.14.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.8.1",
    "@types/socket.io": "^3.0.2",
    "nodemon": "^3.1.7",
    "typescript": "^5.6.3"
  }
}
